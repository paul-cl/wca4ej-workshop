// Assisted by watsonx Code Assistant 
// watsonx Code Assistant did not check whether this code suggestion might be similar to third party code.
```mermaid
sequenceDiagram
    participant User as User
    participant WCA as Watson Code Assistant
    participant WS as WebSphere Application Server
    participant Servlet as WelcomeServlet
    participant DB as Database

    User->>+WCA: /resorts/welcome
    WCA->>+WS: /resorts/welcome
    WS->>+Servlet: /resorts/welcome
    Servlet->>+DB: SELECT * FROM users
    DB-->>-Servlet: user list
    Servlet-->>-WS: user list
    WS-->>-WCA: user list
    WCA-->>-User: user list
```