// Assisted by watsonx Code Assistant 
// watsonx Code Assistant did not check whether this code suggestion might be similar to third party code.
```mermaid
flowchart TB
    %% Watson Code Assistant 서브그래프
    subgraph one
        direction TB
        WCA[Watson Code Assistant]
    end

    %% WebSphere Application Server 서브그래프
    subgraph two
        direction TB
        WS[WebSphere Application Server]
        Servlet[WelcomeServlet]
    end

    %% Database 서브그래프
    subgraph three
        direction TB
        DB[(Database)]
    end

    %% 노드 간 연결 정의 (반드시 subgraph 블록 바깥에서 정의)
    WCA -->|Request: /resorts/welcome| WS
    WS -->|Forward request| Servlet
    Servlet -->|Query: SELECT * FROM users| DB
    DB -->|Response: User list| Servlet
    Servlet -->|Send data| WS
    WS -->|Return response| WCA



```